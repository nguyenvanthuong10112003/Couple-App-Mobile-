<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid
        android:color="@android:color/transparent" /> <!-- Màu nền trong suốt -->
    <stroke
        android:color="@color/primary" android:width="1dp" >
    </stroke> <!-- Màu của đường viền -->
    <corners android:radius="0dp" /> <!-- Độ cong của góc -->
</shape>
